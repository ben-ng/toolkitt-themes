{{#with media}}
  {{#if isVideo}}
    <video id="media_video" class="video-js vjs-default-skin"  
    controls preload="auto" width="auto" height="auto">  
    <source src="{{url}}" type='video/mp4' />
    </video>
  {{else}}
    <img id="media_image" class="pickerHolder img-polaroid" alt="{{name}}" src="{{url}}" />
  {{/if}}
{{/with}}